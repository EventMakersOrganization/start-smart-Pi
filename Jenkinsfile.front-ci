pipeline {
  agent any

  options {
    timestamps()
    disableConcurrentBuilds()
  }

  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }

    stage('Frontend Install') {
      steps {
        dir('frontend') {
          sh 'npm ci'
        }
      }
    }

    stage('Frontend Member3 Tests') {
      steps {
        dir('frontend') {
          sh 'npx ng test --watch=false --browsers=ChromeHeadless --include=**/adaptive-learning.service.spec.ts'
          sh 'npx ng test --watch=false --browsers=ChromeHeadless --include=**/level-test.component.spec.ts'
          sh 'npx ng test --watch=false --browsers=ChromeHeadless --include=**/game-play.component.spec.ts'
        }
      }
    }

    stage('Frontend Member1 Tests') {
      steps {
        dir('frontend') {
          sh 'npx ng test --watch=false --browsers=ChromeHeadless --include=src/app/user-management/auth.service.spec.ts'
          sh 'npx ng test --watch=false --browsers=ChromeHeadless --include=src/app/user-management/jwt.interceptor.spec.ts'
          sh 'npx ng test --watch=false --browsers=ChromeHeadless --include=src/app/user-management/role.guard.spec.ts'
          sh 'npx ng test --watch=false --browsers=ChromeHeadless --include=src/app/user-management/login/login.component.spec.ts'
        }
      }
    }
  }
}
