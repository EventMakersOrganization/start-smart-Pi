apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
    part-of: start-smart
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
